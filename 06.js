// What is Redux-thunk?

/* 
    => Redux-thunk -> old style of redux
    => Redux thunk is a package
    => After installing Redux thunk package we get a middleware -> in case thunk is a middleware
    => Redux thunk stands after the Action and before the Reducer
    => Thunk middleware handle API -> handle asynchronous call -> bind the Action with the data and take it to the reducer
    => in this case we get the data in the store which are come from api/server
*/

// => middle ware stands between an Action taken and the Action executed